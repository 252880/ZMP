#pragma once

#include <cstdio>
#include <sstream>
#include <string>
#include <iostream>

#define LINE_SIZE 500

class Reader
{
private:
  

public:
  Reader(){};
  bool init(std::string cmdFile);
  bool execPreprocesor(std::istringstream &IStrm4Cmds);
};
